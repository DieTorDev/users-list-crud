import styled from 'styled-components';

const SytledMainContainer = styled.main`
	max-width: 700px;
	margin: auto;
	margin-top: 40px;
	display: flex;
	flex-direction: column;
	background-color: white;
	padding: 16px;
`;

const StyledTitle = styled.h1`
	font-weight: 700;
	font-size: 24px;
`;

export { SytledMainContainer, StyledTitle };
